from django.apps import AppConfig


class SgcConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.sgc"
    verbose_name = "Sistema de Gestión de Calidad (ISO)"
