# En dev local con LAN: no la pongas — api.ts detecta el hostname y usa :8000.
# En Vercel/produccion: apunta al backend de Render (HTTPS, sin slash final).
NEXT_PUBLIC_API_URL=https://erp-backend.onrender.com
